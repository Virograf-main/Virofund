
export interface Request {
  name: string;
  email: string;
  image?: string;
  available: boolean; 
  timeAvailable: string;
  details: string;
  department: string;
  role: string;
  departmentColor: string;
  dotColor: string;
}
